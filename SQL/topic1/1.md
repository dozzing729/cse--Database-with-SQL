## SELECT

SELECT 문은 테이블에서 원하는 컬럼의 데이터만 선택해 출력한다

`SELECT (속성) FROM (table)
 WHERE (조건)` 
 
 - 산술식을 포함할 수 있다. 

  ex) 
  ```sql 
  SELECT ID, name, salary/2
  ```
  
 - 집계함수를 사용할 수 있다.

 - AS를 이용하여 각 컬럼에 alias를 설정할 수 있다.
  
  ex) ```SELECT salary / 2 AS 'newSalary'```
  
